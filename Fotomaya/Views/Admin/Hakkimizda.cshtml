@model IEnumerable<Fotomaya.Models.Hakkimizda>
@{
    ViewBag.Title = "Hakkimizda";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="right_col" role="main">
    <div class="">
        <div class="clearfix"></div>
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        <h2>Hakkımızda</h2>
                        <ul class="nav navbar-right panel_toolbox">
                            <li>
                                <a href="/Admin/HakkimizdaEkle"><input type="button" class="btn btn-success" value="Ekle" /></a>
                            </li>
                            <li>
                                <a class="collapse-link" align="right"><i class="fa fa-chevron-up"></i></a>
                            </li>
                        </ul>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <table id="datatable-responsive" class="table table-striped table-bordered dt-responsive nowrap" cellspacing="0" width="100%">
                            <thead>
                                <tr>
                                    <th>Hakkımızda ID</th>
                                    <th>Hakkımızda Başlık</th>
                                    <th>Hakkımızda Açıklama</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(model => item.hakkimizda_id)</td>
                                        <td>@Html.DisplayFor(model => item.hakkimizda_baslik)</td>
                                        <td>@Html.DisplayFor(model => item.hakkimizda_aciklama)</td>
                                        <td>
                                            <a href='@Url.Action("HakkimizdaDuzenle","Admin", new { id=item.hakkimizda_id })' class="btn btn-success">Düzenle</a>
                                        </td>
                                        <td>
                                            <a href='@Url.Action("HakkimizdaSil","Admin", new { id=item.hakkimizda_id })' class="btn btn-danger">Sil</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>